import loadExpress from "./express";

export default async ({ app }) => {
  // Load Database
  //   Load Model

  // TODO: Add Logger
  // Load all middlewares

  // Load Express
  await loadExpress({ app });
};
